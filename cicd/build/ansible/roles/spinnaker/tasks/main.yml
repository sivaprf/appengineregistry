---
# - { include: tasks/tasks_sudo.yml, become: yes, become_method: sudo }
# - { include: tasks/tasks_builder.yml, become: yes, become_user: builder }
# - { include: tasks/tasks_configure.yml, become: yes, become_user: builder }
- { include: tasks/tasks_deploy.yml, become: yes, become_user: builder }
